{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -c \"import os,sys; paths=[os.getcwd(),'/app','/workspace','/var/task','/usr/src/app','/opt','/home']; root=next((p for p in paths if p and os.path.exists(os.path.join(p,'server','main.py'))), None);\nif root is None: sys.exit('Could not locate server package');\nsys.path.insert(0, root); from server.main import main; main()\"",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
