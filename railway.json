{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -c \"import os,sys; from pathlib import Path; cwd=Path(os.getcwd()); candidates=[cwd] + [cwd / d for d in os.listdir(cwd) if (cwd / d).is_dir()]; root=None; \nfor p in candidates:\n    if (p / 'server' / 'main.py').exists():\n        root=p\n        break\nif root is None:\n    raise SystemExit('Could not locate server package')\nsys.path.insert(0, str(root)); from server.main import main; main()\"",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
